{% extends 'base_agri.html.twig' %}

{% block body %}

<div class="card">
    <div class="card-header">
        <button type="button" class="btn btn-primary" onclick="window.location.href='/lemca/achat/0'">Ajouter</button>
        Achats
    </div>
    <table class="table">
        <tr>
            <th>date</th>
            <th>name</th>
            <th>file</th>
            <th>count</th>
        </tr>
        {% for binary in binaries %}
            <tr>
                <td>{{ binary.date | showDatetime }}</td>
                <td>{{ binary.name }}</td>
                <td><a href="/lemca/bineuse_binary?branch={{binary.name}}">{{ binary.filename }}</td>
                <td></td>
            </tr>
        {% endfor %}
    </table>
</div>

{% endblock %}
