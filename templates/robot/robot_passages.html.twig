{% extends 'base_agri.html.twig' %}

{% block body %}

<div class="card">
    <div class="card-header">
        Robots
    </div>
    <table class="table">
        <tr>
            <th>Nom</th>
            <th></th>
        </tr>
        {% for p in passages %}
            <tr>
                <td>{{p.datetime | showDatetime2}}</a></td>
                <td>{{p.l1}}</td>
                <td>{{p.l2}}</td>
                <td>{{p.l3}}</td>
                <td>{{p.l4}}</td>
            </tr>
        {% endfor %}
    </table>
</div>



{% endblock %}
