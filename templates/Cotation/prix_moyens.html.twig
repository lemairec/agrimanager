{% extends 'base_agri.html.twig' %}

{% block body %}


<div class="card">
    <div class="card-header">
        <button type="button" class="btn btn-primary" onclick="window.location.href='{{path('prix_moyen', {'id': 0})}}'">Ajouter</button>
        Produits
    </div>
    <table class="table">
        <tr>
            <th>Produit</th>
            <th>Catégorie</th>
            <th></th>
        </tr>
        {% for prix_moyen in prix_moyens %}
            <tr>
                <td><a href="{{path('prix_moyen', {'id': prix_moyen.id})}}">{{ prix_moyen.produit }}</a></td>
                <td>{{ prix_moyen.campagne }}</td>
                <td>{{ prix_moyen.source }}</td>
                <td>{{ prix_moyen.getPrixTotal }}</td>
            </tr>
        {% endfor %}
    </table>
</div>

{% endblock %}
