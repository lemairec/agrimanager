{% extends 'base_agri.html.twig' %}

{% block body %}


<div class="card">
    <table class="table">
        <tr>
            <th></th>
            <th></th>
        </tr>
        {% for k,t in res %}
            <tr>
                <td>{{ t["value"] }}</td>
                <td>{{ t["sum"] | showEur }}</td>
            </tr>
        {% endfor %}

    </table>
</div>

{% endblock %}
