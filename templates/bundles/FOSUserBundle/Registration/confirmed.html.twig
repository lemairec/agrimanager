{% extends 'base_agri.html.twig' %}

{% trans_default_domain 'FOSUserBundle' %}

{% block fos_user_content %}
<div class="d-flex align-items-center bd-highlight mt-5">
    <div class="container">
        <div class="row d-flex justify-content-center">
            <div class="col-xs-10 col-sm-8 col-md-6 col-lg-4 mt-2">
                <div class="card ">
                    <div class="card-body">
                        <p>{{ 'registration.confirmed'|trans({'%username%': user.username}) }}</p>
                        {% if targetUrl %}
                        <p><a href="{{ targetUrl }}">{{ 'registration.back'|trans }}</a></p>
                        {% endif %}
                    </div>
                    <div class="card-body">
                        <p>Vous pouvez maintenant commencer par crée une parcelle <a href="{{url("parcelle", {'parcelle_id':0})}}">ici</a>.</p>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>
{% endblock fos_user_content %}
