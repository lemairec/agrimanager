<nav class="navbar navbar-expand-lg navbar-dark fixed-top bg-dark  mb-4">

        <a class="navbar-brand" href="{{ url('home') }}">MaPlaine</a>
        <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav mr-auto">
                {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                    {% if is_granted('ROLE_ADMIN') %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            {{'Admin' | trans}}
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <a class="dropdown-item" href="{{ url('admin_users') }}">Users</a>
                            <a class="dropdown-item" href="{{ url('admin_companies') }}">Companies</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{ url('admin') }}">Easy Admin</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{ url('annonces') }}">Annonces</a>
                            <a class="dropdown-item" href="{{ url('annonces_immobilier') }}">Immobilier</a>
                        </div>
                    </li>
                    {% endif %}
                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            Société
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <a class="dropdown-item" href="{{ url('campagnes') }}">Campagnes</a>
                            <a class="dropdown-item" href="{{ url('ilots') }}">Ilots</a>
                            <a class="dropdown-item" href="{{ url('cultures') }}">Cultures</a>
                            <a class="dropdown-item" href="{{ url('analyse_sols') }}">Analyses Sol</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{ url('materiels') }}">Materiel</a>
                            {% if app.user.username == "lejard" %}
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{ url('gasoils') }}">Gasoil</a>
                            <a class="dropdown-item" href="{{ url('deplacements') }}">Deplacements</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{ url('appartement_operations') }}">Appartement Opérations</a>
                            <a class="dropdown-item" href="{{ url('appartement_bilan') }}">Bilan</a>
                            {% endif %}

                        </div>
                    </li>
                    <li class="nav-item"><a class="nav-link" href="{{ url('parcelles') }}">Parcelles</a></li>
                    <li class="nav-item"><a class="nav-link" href="{{ url('interventions') }}">Interventions</a></li>

                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            Produits
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <a class="dropdown-item" href="{{ url('produits') }}">Produits</a>
                            <a class="dropdown-item" href="{{ url('achats') }}">Achats</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{ url('stocks') }}">Stocks</a>
                            <a class="dropdown-item" href="{{ url('produit_campagnes') }}">Prix campagne</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{ url('ephy_produits') }}">Produits Ephy</a>
                        </div>
                    </li>

                    <li class="nav-item"><a class="nav-link" href="{{ url('calendar') }}">Calendrier</a></li>

                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            Bilan
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <a class="dropdown-item" href="{{ url('bilan_charges') }}">Bilan charges</a>
                            <a class="dropdown-item" href="{{ url('bilan_detail') }}">Bilan Detaillé</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{ url('bilan') }}">Bilan</a>
                            <a class="dropdown-item" href="{{ url('bilan_rendements') }}">Rendements</a>
                            <a class="dropdown-item" href="{{ url('bilan_produits') }}">Produits</a>
                            <a class="dropdown-item" href="{{ url('bilan_comptes') }}">Comptes</a>
                            <a class="dropdown-item" href="{{ url('bilan_gasoil') }}">Gasoil</a>
                            <a class="dropdown-item" href="{{ url('bilan_dates') }}">Dates</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{ url('engrais') }}">Bilan engrais</a>

                        </div>
                    </li>

                    {% if app.user.username == "lejard" %}

                    <li class="nav-item"><a class="nav-link" href="{{ url('livraisons') }}">Livraisons</a></li>

                    <li class="nav-item dropdown">
                        <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                            Gestion
                        </a>
                        <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                            <a class="dropdown-item" href="/cours">Cours</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{ url('comptes') }}">Comptes</a>
                            <a class="dropdown-item" href="{{ url('banque') }}">Banque</a>
                            <div class="dropdown-divider"></div>
                            <a class="dropdown-item" href="{{ url('operations') }}">Operations</a>
                            <a class="dropdown-item" href="{{ url('factures_fournisseurs') }}">Factures</a>
                        </div>
                    </li>
                    {% endif %}

                {% endif %}
            </ul>
            <ul class="nav navbar-nav navbar-right">
                <!--button(type='button', class='btn btn-default navbar-btn') Sign in-->
                <li class="dropdown">
                    <a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false">
                        Profile
                    </a>
                    <div class="dropdown-menu" aria-labelledby="navbarDropdown">
                        {% if is_granted('IS_AUTHENTICATED_REMEMBERED') %}
                            <a class="dropdown-item"  href="{{ url('fos_user_security_logout') }}">Deconnexion</a>
                            <a class="dropdown-item"  href="{{ url('fos_user_profile_edit') }}">{{ app.user.username }}</a>
                        {% else %}
                            <a class="dropdown-item"  href="{{ url('fos_user_security_login') }}">{{ 'base.Login'|trans }} </a>
                            <a class="dropdown-item"  href="{{ url('fos_user_registration_register') }}">{{ 'base.Register'|trans }} </a>
                        {% endif %}

                    </div>
                </li>
            </ul>

        </div>

</nav>
