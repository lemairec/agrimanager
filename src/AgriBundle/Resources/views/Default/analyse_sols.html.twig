{% extends 'AgriBundle::base_agri.html.twig' %}

{% block body %}


<div class="container bs-docs-container">
    <div class="panel panel-default", style="width:100%">
        <!-- Default panel contents -->
        <div class="panel-heading">
            <button type="button" class="btn btn-primary" onclick="window.location.href='/achat/0'">ADD</button>
        </div>
        <table class="table">
            <tr>
                <th>Name</th>
            </tr>
            {% for analyse_sol in analyse_sols %}
            <tr>
                <td><a href="{{ url("analyse_sol", {'analyse_sol_id': analyse_sol.id}) }}">{{analyse_sol.id}}</td>
            </tr>
            {% endfor %}
        </table>
    </div>
</div>
{% endblock %}
